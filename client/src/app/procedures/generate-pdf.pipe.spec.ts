import { GeneratePdfPipe } from './generate-pdf.pipe';

describe('GeneratePdfPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneratePdfPipe();
    expect(pipe).toBeTruthy();
  });
});
